# backend

To install dependencies:

```bash
bun install
```

To run:

```bash
bun run index.ts
```

This project was created using `bun init` in bun v1.1.20. [Bun](https://bun.sh) is a fast all-in-one JavaScript runtime.

---

Tava usando, parei com o bun e migrei com express porque essa porcaria tava bloqueando meus requests.